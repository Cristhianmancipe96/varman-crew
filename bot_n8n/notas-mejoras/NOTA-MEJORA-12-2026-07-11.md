# NOTA MEJORA 12 — [E2b] Más textos cálidos: bienvenida y cierre (flag BOT_TEXTOS_V2) · 2026-07-11

## Qué cambié
Dos textos más en el bloque de override de `BOT_TEXTOS_V2` (final de
`workflows/src/textos.js`), los 2 de mayor tráfico del embudo:

1. **`categoriasBody` (bienvenida — la ve TODA conversación):** versión BANCO §1
   adaptada a la lista de categorías: "¡Hola! Bienvenido a *VarMan Crew* 👟
   Cuéntame, ¿qué modelo tienes en la mira? Escríbeme la *marca* que buscas, o
   mira nuestros estilos aquí abajo 👇". OJO: invita a escribir la MARCA (el
   camino `buscar_marca` funciona siempre) y NO a mandar foto, para no acoplar
   este texto al flag `BOT_FOTO_ASESOR`.
2. **`pedidoRecibido` (cierre — lo ve TODA venta con comprobante):** conserva
   "¡Pedido recibido!" + `{ref}/{talla}/{total}` y suma los ganchos de confianza
   del BANCO §12-13: *caja original y bien protegido* + *guía de rastreo*.

Con el flag OFF, ambos textos = v5 exactos (el override no corre).

## Tests (ampliación de la sección 34)
- OFF: bienvenida v5 ("Estos son nuestros estilos").
- ON: bienvenida BANCO §1 ("qué modelo tienes en la mira" + marca).
- ON: cierre con "caja original" + "guía de rastreo" conservando "Pedido
  recibido" y la ref ("Referencia 05").

Resultado: **184 PASS · 0 FAIL** (antes 181; +3 checks).

## Pendientes del ítem E2b (para otra vuelta)
`comprarIntro`, `datosFaltan`/`datosIncompletos` (BANCO §9), `refDirectaIntro`.

## Cómo revertir
1. Copiar `workflows/respaldo/bot-varman.pre-mejora-12.json` sobre `workflows/bot-varman.json`.
2. Quitar las 2 líneas nuevas del bloque `BOT_TEXTOS_V2` en `textos.js` y los 3
   checks E2b de la sección 34 (+ su limpieza `mPathE2B`).
3. `node workflows/build-v4-pedidos.js`.

## ⚙️ Entorno
Node portable en `bot_n8n/herramientas/node/node.exe`. Ver `NOTA-MEJORA-1`.
